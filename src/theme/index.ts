import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#fff',
    },
  },
  typography: {
    fontSize: 22,
    fontFamily: "'Montserrat', sans-serif",
  },
});
